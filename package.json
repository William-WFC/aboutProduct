{
  "scripts": {
    "dev": "webpack-dev-server --inline --progress",
    "start": "npm run dev",
    "build": "node build/build.js",
    "all": "npm run build -- sx && npm run build -- wl && npm run build -- hx"
  },
  "dependencies": {
    "webpack": "^4.12.1",
    "webpack-dev-server": "^3.1.4"
  },
  "devDependencies": {
    "webpack-cli": "^3.0.8"
  }
}
